<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clawbotforall.auth.SessionMapper">
  <insert id="insert" parameterType="com.clawbotforall.auth.SessionEntity">
    INSERT INTO admin_sessions (id, admin_id, created_at, expires_at)
    VALUES (#{id}, #{adminId}, #{createdAt}, #{expiresAt})
  </insert>

  <delete id="deleteById">
    DELETE FROM admin_sessions
    WHERE id = #{id}
  </delete>

  <delete id="deleteExpired">
    DELETE FROM admin_sessions
    WHERE expires_at &lt;= #{now}
  </delete>

  <select id="findAdminBySessionId" resultMap="com.clawbotforall.auth.AdminMapper.AdminMap">
    SELECT a.*
    FROM admin_sessions s
    JOIN admins a ON a.id = s.admin_id
    WHERE s.id = #{sessionId}
      AND s.expires_at &gt; #{now}
    LIMIT 1
  </select>
</mapper>
