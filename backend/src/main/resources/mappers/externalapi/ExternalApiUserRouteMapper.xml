<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clawbotforall.externalapi.ExternalApiUserRouteMapper">
  <resultMap id="ExternalApiUserRouteMap" type="com.clawbotforall.externalapi.ExternalApiUserRouteEntity">
    <id property="openidHash" column="openid_hash"/>
    <result property="openid" column="openid"/>
    <result property="openvikingUserId" column="openviking_user_id"/>
    <result property="instanceId" column="instance_id"/>
    <result property="createdAt" column="created_at"/>
    <result property="updatedAt" column="updated_at"/>
    <result property="lastUsedAt" column="last_used_at"/>
  </resultMap>

  <select id="findByOpenidHash" resultMap="ExternalApiUserRouteMap">
    SELECT *
    FROM external_api_user_routes
    WHERE openid_hash = #{openidHash}
    LIMIT 1
  </select>

  <select id="list" resultMap="ExternalApiUserRouteMap">
    SELECT *
    FROM external_api_user_routes
    WHERE
      (#{keyword} IS NULL OR #{keyword} = ''
       OR openid LIKE CONCAT('%', #{keyword}, '%')
       OR openviking_user_id LIKE CONCAT('%', #{keyword}, '%'))
      AND (#{instanceId} IS NULL OR #{instanceId} = '' OR instance_id = #{instanceId})
    ORDER BY last_used_at DESC, created_at DESC
    LIMIT #{limit} OFFSET #{offset}
  </select>

  <select id="count" resultType="int">
    SELECT COUNT(*)
    FROM external_api_user_routes
    WHERE
      (#{keyword} IS NULL OR #{keyword} = ''
       OR openid LIKE CONCAT('%', #{keyword}, '%')
       OR openviking_user_id LIKE CONCAT('%', #{keyword}, '%'))
      AND (#{instanceId} IS NULL OR #{instanceId} = '' OR instance_id = #{instanceId})
  </select>

  <select id="countByInstanceId" resultType="int">
    SELECT COUNT(*)
    FROM external_api_user_routes
    WHERE instance_id = #{instanceId}
  </select>

  <insert id="insert" parameterType="com.clawbotforall.externalapi.ExternalApiUserRouteEntity">
    INSERT INTO external_api_user_routes
      (openid, openid_hash, openviking_user_id, instance_id, created_at, updated_at, last_used_at)
    VALUES
      (#{openid}, #{openidHash}, #{openvikingUserId}, #{instanceId}, #{createdAt}, #{updatedAt}, #{lastUsedAt})
  </insert>

  <update id="updateLastUsed">
    UPDATE external_api_user_routes
    SET last_used_at = #{lastUsedAt}, updated_at = #{lastUsedAt}
    WHERE openid_hash = #{openidHash}
  </update>

  <update id="updateInstance">
    UPDATE external_api_user_routes
    SET instance_id = #{instanceId}, updated_at = #{updatedAt}
    WHERE openid_hash = #{openidHash}
  </update>
</mapper>
